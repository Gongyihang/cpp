#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <math.h>
using namespace std;

int main(){
    int n;
    while(scanf("%d",&n) != EOF){
        vector<int> v(n);
        for(int i = 0;i < n;i++){
            scanf("%d",&v[i]);
        }
        
    }
    system("pause");
    return 0;
}